{% extends 'base.html.twig' %}

{% block title %}Edit Reservation{% endblock %}

{% block body %}
    <h1>Edit Reservation</h1>

    {{ include('reservation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_reservation_index') }}">back to list</a>

    {{ include('reservation/_delete_form.html.twig') }}
    <!-- Même code que new.html.twig, juste changer le titre et le bouton -->
    <h2>Modifier votre réservation</h2>
    <!-- ... le reste identique ... -->
    <button type="submit" class="btn-getstarted btn-lg px-5">
        Enregistrer les modifications
    </button>
{% endblock %}
